# Runtime dependencies for the MVP walking skeleton.
# Pinned to lower bounds; use `pip install -e .[dev]` for the resolved set.
fastapi>=0.111
pydantic>=2.6
uvicorn>=0.29

# Dev / test
pytest>=8.0
httpx>=0.27
